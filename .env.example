# Server-side URLs. The browser never receives Relay credentials.
AUTH_URL=https://auth.zmzai.cloud
MONGODB_URI=mongodb+srv://.../zmzai_sandbox
RELAY_URL=https://m.zmzai.cloud/api/v1
# 私网 Relay 地址；生产环境必须设置服务密钥。开发环境可暂时指向 Relay 公网地址。
RELAY_INTERNAL_URL=http://127.0.0.1:3002
RELAY_SANDBOX_SERVICE_SECRET_CURRENT=replace-with-shared-server-side-secret
OPEN_SANDBOX_URL=http://127.0.0.1:8080
OPEN_SANDBOX_API_KEY=replace-with-server-side-key
OPEN_SANDBOX_PROTOCOL=http
OPEN_SANDBOX_IMAGE=node:22-alpine
OPEN_SANDBOX_CPU_LIMIT=500m
OPEN_SANDBOX_MEMORY_LIMIT=512Mi
# a.zmzai.cloud -> z.zmzai.cloud 服务间认证（内部 Agent API）。
# 只保存在两端服务端环境，绝不进入浏览器或用户 sandbox_key 流程。
SANDBOX_AGENT_SERVICE_SECRET_CURRENT=replace-with-agent-service-secret
SANDBOX_AGENT_SERVICE_SECRET_PREVIOUS=
# exec 工具允许的程序白名单（逗号分隔，覆盖默认列表）。
SANDBOX_AGENT_ALLOWED_PROGRAMS=
# 运行历史归档保留时长（小时）；0 表示不持久化，仅进程内存。
SANDBOX_RUN_TTL_HOURS=24
# Agent 多轮规划的最大执行步数（1-8）。
SANDBOX_MAX_PLAN_STEPS=3

# 遥测：span 事件推送到 zmzai-billing /api/v1/ingest（未配置时静默跳过）
BILLING_INGEST_URL=
BILLING_INGEST_KEY=
